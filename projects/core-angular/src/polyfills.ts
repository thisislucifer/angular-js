//Before angular loads our app, it make sure all our application, all the decorators are at its proper place
// as its hunting for the selector that we have mentioned in components. All this things are done by this polyfills.ts file.
import "zone.js/dist/zone";